#include <stdio.h>

int main() 
{
    int n,i;
    int sum = 0;
    int fact= 1; 
    printf("Enter a value for n: ");
    scanf("%d", &n);
    for (i = 1; i <= n; i++) 
	{
        fact*= i;
        sum += fact; 
    }
    printf("The sum of the series is: %d\n", sum);

    return 0;
}
