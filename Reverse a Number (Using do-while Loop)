#include<stdio.h>
int main()
{
	int num,rev,r;
	printf("enter a no: ");
	scanf("%d",&num);
	 do 
	{
        r= num % 10;       
        rev = rev * 10 + r;  
        num = num / 10;    
    } 
	while (num != 0); 
	printf("reversed no is:%d ",rev);
	return 0;
}
